# Screenshot

Save a dashboard PNG here as `screenshot.png` so the README image resolves.

Suggested frame: sidebar (filters + weights) visible, ranked table with color scores, and one ticker’s “why this score” + margin chart in view.
